# AI 使用说明

这份文档说明 AI 在本作业中的实际参与方式、我否决了哪些 AI 建议、以及哪些关键决策是我自己做的。

我的基本用法是：**把 AI 当成一个知识面很广但没有上下文的结对伙伴**。
它很擅长把某个领域的常见做法一次性摆在我面前，也很擅长把我已经想清楚的东西快速变成代码。
但它默认会给"教科书上最完整的方案"，而工程判断恰恰在于**决定哪些部分不要**。
本项目里我花在删减上的精力，多于花在增加上的精力。

## 一、AI 在哪些关键地方提供了帮助

### 1. 方案空间的快速铺开（最大价值）

我在动手前让 AI 列举"可靠 webhook 投递"的主流架构方案。它给出了从
「DB 当队列 + 轮询」到「Kafka + Scheduler + Redis 限流 + Outbox + CDC」的完整谱系。

这一步的价值不在于它推荐了哪个，而在于**让我一次性看到了整个选择空间和每个选项的代价**。
我自己想大概能想到三四种，AI 给了八九种。最终我选了最简单的那个，
但这个选择是"看过全部选项之后的选择"，而不是"只知道一种做法"。

### 2. 边界情况的清单式提醒

AI 在几个地方提醒了我容易漏的细节，这些确实帮我省了排查时间：

- **`Retry-After` 有两种格式**：秒数和 HTTP 日期。我原本只打算解析秒数。
- **响应体必须读到 EOF 才能复用连接**。只 `Close()` 不 `ReadAll` 会让连接池失效，
  高频投递退化成每次新建 TCP+TLS。这是个很隐蔽的性能坑。
- **Go 的 `MaxIdleConnsPerHost` 默认只有 2**。对持续向少量固定域名发请求的服务过小。
- **`math.Pow` 在 attempt 很大时会溢出成 `+Inf`**，转 `time.Duration` 后是未定义值。
  我为此写了 `TestNextBackoffHandlesHugeAttemptWithoutOverflow`。
- **Prometheus 标签值需要转义**引号、反斜杠、换行，否则输出无法被解析。

这类"已知陷阱清单"是 AI 最稳定可靠的贡献——它见过的坑比我多。

### 3. 把已定方案翻译成代码

在我定好状态机、表结构、调度模型之后，AI 帮我快速生成了大量样板代码：
SQL 语句与 `Scan` 映射、`http.Handler` 骨架、配置结构体的字段级 fallback、
Prometheus 文本格式的渲染逻辑。这部分我基本是审阅和调整，节省了大量打字时间。

### 4. 测试用例的补全

我先写了主干测试（幂等、lease、退避、熔断、端到端故障恢复），
然后让 AI 帮我找遗漏的分支。它补出了几个我确实没想到的：
`VerdictIgnore` 必须归还半开探测额度（否则半开永久卡死）、
空库时 `CountByStatus` 要为所有状态输出 0、
显式配了 `success_status_codes` 后 200 就不该算成功。

第二条我一开始觉得是吹毛求疵，想清楚后发现很实际：
指标序列缺失会让告警规则无法区分"从未出现过死信"和"死信指标挂了"。这条建议我采纳了。

### 5. 文档表达的打磨

README 的结构和部分表格是我和 AI 一起调整的。
但**所有技术判断和取舍理由都是我自己写的**——
这部分如果交给 AI，写出来的会是正确但没有观点的通用论述。

## 二、AI 给出过、但我没有采纳的建议

这一节是我认为最值得讨论的部分。我把它分成两类：
**架构层面的"过度设计"**，和**细节层面的"看起来对但实际错"**。

### 2.1 架构层面：AI 默认给"最完整"，我要的是"刚好够"

AI 最初给出的第一版架构是这样的：

> Kafka（主队列）+ 延迟 Topic（重试）+ 死信 Topic + 独立 Scheduler 服务（时间轮）
> + Redis（分布式限流 + 熔断状态共享）+ PostgreSQL（状态存储）
> + Outbox 表 + Debezium CDC + OpenTelemetry + Jaeger + 每 endpoint 独立 Deployment

**我全部没采纳。** 逐条说理由：

| AI 建议 | 我的判断 |
|---|---|
| **Kafka + 延迟 Topic + 死信 Topic** | 通知投递需要的是延迟重试、单条状态查询、人工重投。这三件事用 SQL 表天然就有，用 Kafka 每一件都要额外机制。而且引入 MQ 后状态在 DB、消息在 MQ，多出一个双写一致性问题。**第一版的瓶颈不是吞吐，不该为不存在的瓶颈付运维成本。** |
| **独立 Scheduler 服务** | 拆出一个服务要付出部署、监控、发布、服务间调用的全套成本，换来的只是"职责更清晰"。进程内一个 goroutine 循环就能做完，而且 `Store` 接口已经让未来拆分的成本可控。 |
| **Redis 共享熔断状态** | 为了多实例熔断状态一致，引入一个新的可用性依赖。我反问了自己一个问题：**Redis 挂了熔断器怎么办？** 每实例独立熔断最坏情况只是生效慢一点，而不是错。用一个新的故障点换一点一致性，不值得。 |
| **Outbox + CDC** | 这条是 AI 对问题场景的**误判**，也是我印证"不能盲信 AI"最典型的一次。Outbox 解决的是「业务数据库事务与消息投递的原子性」，那是**业务系统侧**的问题。本系统是接收方，入口就是一次 HTTP 调用，天然不存在跨库双写。我指出后 AI 承认了，但如果我不了解 Outbox 的适用前提，很可能就把它加进去了。 |
| **每 endpoint 独立 Deployment** | 故障隔离的目标我用进程内的独立并发池 + 独立熔断器达到了，代价是几行代码。拆成 N 个部署的代价是 N 倍的运维负担。**同一个目标，先选代价最小的手段。** |
| **OpenTelemetry + Jaeger** | 本系统的链路只有两跳。用 `X-Notification-Id` 贯穿全程 + 完整的 attempt 审计记录，已经能回答所有排障问题。真要接 OTel 应该和公司统一方案一起做，不是在这里单独引入。 |
| **Exactly-once（事务消息 / 2PC）** | 这是我最坚决否决的一条。它承诺了做不到的事——下游不配合幂等时，任何协议都无法保证恰好一次。我选择**诚实地提供 at-least-once，并主动为下游去重创造条件**。一个有漏洞的 exactly-once 比一个边界清楚的 at-least-once 危险得多，因为它会让下游放弃自己的幂等设计。 |
| **JSONata / Jolt 转换 DSL** | AI 想把"Body 格式不同"做成一个通用转换引擎。我认为这条边界划错了：payload 映射是业务语义，放进通知服务意味着通知服务的发布风险覆盖所有业务方。我只保留了一个 30 行的 `text/template` 逃生舱，并在配置注释里写明"这不是通用转换引擎"。 |
| **Event Sourcing / CQRS** | 状态机只有 5 个状态、一张表就能表达。事件溯源会把"查一条通知现在什么状态"从一次主键查询变成一次事件重放——而这正是运维最高频的操作。 |
| **优先级队列** | 需求里没有任何优先级概念。加了之后要回答"低优先级饥饿怎么办"，凭空增加一类问题。 |
| **管理后台 Web UI** | 死信巡检和重投用几个 JSON 接口就能完成。UI 是真实痛点出现后再做的事。 |
| **配置热加载** | 要处理"配置变更时在途任务用新配置还是旧配置"，是真实的复杂度。而重启不丢消息（优雅关闭已有测试覆盖），接入频率又低。 |

### 2.2 细节层面：看起来合理但我判断是错的

这几条更能说明"不能照抄 AI"，因为它们表面上都很有道理。

**① AI 建议 401/403 应该重试（可能是 token 刷新中）**

不采纳。这个理由在极少数场景成立，但代价不对称：
如果真是配置错了（绝大多数情况），重试 8 次只是把"需要人介入"推迟几十分钟；
如果真是 token 刷新，一次失败后等下一批通知也能自愈。
**让错误快速可见比让它悄悄自愈更重要。**
我做成了可配置（`retry_status_codes`），默认判永久失败。

**② AI 建议 4xx 也应该计入熔断失败计数**

不采纳，而且我认为这是个会引发真实事故的建议。
熔断器度量的是「下游是否可用」，4xx 恰恰证明下游活着——能连接、能解析、能回响应。
如果 4xx 计入熔断，某个业务方推一批脏数据（大量 400）就能熔断整条通道，
把其他业务方的正常通知全部堵住。

我为此在代码里专门引入了 `Verdict`（健康/不健康/忽略）这个概念，
和业务层的 `Outcome`（成功/可重试/永久失败）显式分开，
并写了 `TestBusinessFailuresDoNotTripTheBreaker` 锁住行为。
**这是我自己做的一个关键设计决策，直接来自对 AI 建议的否决。**

**③ AI 的熔断器实现是"照常取任务、直接判失败、attempt+1"**

这是 AI 给的第一版熔断代码，也是很多开源实现的做法。但它有个严重后果：
下游宕机 10 分钟就能把所有在途通知的重试预算全部烧完、全部推进死信——
而这些通知本来只要等下游恢复就能成功。

我改成了**熔断打开时任务根本不从队列里领取**，因此不消耗预算。
熔断的意义是"先别发，等一等"，不是"快速把失败次数用完"。
`TestCircuitBreakerStopsHammeringAndPreservesRetryBudget` 显式断言了熔断期间 `attempt` 不变。

**④ AI 建议 lease 过期回收时不增加 attempt（"这次失败不是通知的错"）**

不采纳。听起来公平，但会留下一个危险漏洞：
一条能稳定打挂进程的"毒任务"会被无限次回收重放，
把单条脏数据放大成全系统不可用。
我选择让崩溃也消耗预算，预算耗尽进死信等人工处理——
**把故障限制在一条记录内，比对单条记录"公平"更重要。**

**⑤ AI 建议用 `MaxOpenConns(N)` 提升 SQLite 并发**

不采纳。SQLite 同一时刻只允许一个写者，多连接抢写锁只会换来 `SQLITE_BUSY` 和难复现的抖动。
我设成 1，让写串行化，换取零锁错误和完全可预测的行为。
这也正好给出了迁 PostgreSQL 的明确触发条件。

**⑥ AI 建议做自动死信重投（定时扫描死信重新投递）**

不采纳，这条我想得最久。
通知会进死信，说明**自动重试已经失败过了**。再加一层自动重试只是把同样的失败推迟发生，
同时掩盖了"有东西坏了需要人看"这个信号。
死信应该是一个刺眼的告警，不是一个会自己消失的状态。

**⑦ AI 最初把调用方传入的 URL 作为请求参数**

不采纳。这是个安全问题：允许调用方指定任意 URL，等于在内网开了一个 SSRF 探测代理。
我改成了 endpoint 注册制（调用方只给逻辑名），
顺带还解决了凭据集中管理和"供应商换地址时业务方不用发版"两个问题。

**⑧ AI 建议直接用 `endpoint` 请求参数作为 Prometheus 标签值**

不采纳。未注册的 endpoint 名来自调用方输入，直接当标签值会造成标签基数爆炸——
一次拼写错误的循环调用就能生成上万条时间序列，把监控系统打挂。
我统一归到 `_unknown`，并写了测试断言。

**⑨ AI 建议引入 `prometheus/client_golang`、`gin`、`zap`、`viper`、`uuid`**

全部不采纳。这个项目最终只有 2 个直接依赖（`yaml.v3` 和 SQLite 驱动）。
路由用 Go 1.22 的 `http.ServeMux`（已支持方法和路径参数）、
日志用标准库 `log/slog`、配置用 `yaml.v3` 直接解析、
ID 自己实现（顺带拿到了时间有序这个 UUIDv4 给不了的好处）、
指标手写 200 行。

判断依据是：**依赖不是免费的**，它们会出现在每一次安全扫描和版本升级里。
只有当自己实现的成本明显高于依赖的维护成本时才引入。
同时我也给手写指标写明了**失效条件**（需要 exemplar / 原生直方图 / pushgateway 时就换官方库），
让后来的人知道什么时候该推翻这个决定。

**⑩ AI 生成的代码里，超时用的是 `http.Client{Timeout: ...}`**

改掉了。一个全局超时无法同时适配"快返回的广告系统"（3s）和"慢的 CRM"（10s）。
我改成 per-request 的 `context` 超时，从 endpoint 配置读取。

## 三、哪些关键决策是我自己做出的，以及原因

下面每一条都是我自己拍的，其中多条是**在否决 AI 建议之后**才形成的设计。

### 1. 用数据库当队列，第一版不引入任何中间件

**决策**：持久化队列就是一张 SQL 表，`next_attempt_at` 字段承担延迟重试，lease 承担崩溃恢复。

**原因**：我先问自己「这个系统的队列语义特殊在哪」，答案是四点——
延迟重试、按 key 幂等、单条状态可查、死信可人工重投。
这四点用 SQL 表天然都有，用 MQ 每一点都要额外机制。
而且通知系统的核心价值是**状态可查、可人工干预**，这需要随机读单条记录，
正是数据库擅长、MQ 不擅长的。第一版的瓶颈不是吞吐，不该为不存在的瓶颈付运维成本。

我同时为演进做好了准备（`Store` 接口 + 表结构可直接搬到 PostgreSQL），
并写明了触发条件（SQLite 写入延迟上升 / 队列深度持续增长）。

### 2. 熔断期间不消耗重试预算

**决策**：熔断打开时任务根本不从队列里领取，`attempt` 不增加。

**原因**：这是我对 AI 熔断实现的修正，也是我认为整个项目里最有价值的一个判断。
如果照常取任务再判失败，下游宕机 10 分钟就能把所有在途通知的预算烧完推进死信——
而它们本来只要等下游恢复就能成功。熔断的语义是"先别发"，不是"快速失败完"。

### 3. 把「业务失败」和「下游不可用」显式分成两个概念

**决策**：代码里 `Outcome`（成功/可重试/永久失败，业务视角）和
`Verdict`（健康/不健康/忽略，可用性视角）是两套独立的枚举。4xx 是 `Outcome=permanent` 但 `Verdict=healthy`。

**原因**：来自否决 AI 的"4xx 计入熔断"建议。
用业务失败去触发熔断，会让一个业务方的脏数据熔断掉整条通道、连带影响所有其他业务方。
把这两个维度混成一个布尔值是很多熔断实现的隐藏 bug，我选择在类型层面就把它们分开，
让后来改代码的人不可能不小心混用。

### 4. 4xx 立刻进死信，不消耗重试预算

**决策**：除 408/423/425/429 和全部 5xx 之外的非成功状态码，一律判永久失败直接进死信。

**原因**：`401` 重试十次不会突然有权限，`404` 重试十次地址不会突然存在。
把它们和 5xx 一样重试，唯一效果是把"需要人介入"推迟几十分钟才暴露。
**我认为一个通知系统的质量，很大程度体现在它多快让人知道"这条真的不行"。**

### 5. 按 endpoint 分别领取任务 + 每 endpoint 独立并发池

**决策**：调度循环遍历每个 endpoint，各自算额度、各自领取、各自独立信号量和熔断器。

**原因**：这是故障隔离的实现方式，也是我对"一个全局 worker 池"这个默认做法的主动否决。
全局池下一个卡死的供应商会占满池子，拖垮所有人（队头阻塞）。
需求里三个供应商的可靠性显然不同（自研库存系统 vs 第三方广告系统），
隔离不是可选项。我为此写了 `TestSlowVendorDoesNotBlockOtherEndpoints` 作为验收标准。

### 6. endpoint 注册制：调用方不能传 URL

**决策**：业务系统只提交逻辑名，URL/凭据/策略全在服务配置。

**原因**：来自对 AI 初版接口设计的安全修正。一个参数同时解决了三件事：
防 SSRF、凭据集中管理、供应商变更时业务方不用发版。
代价（接入新供应商要改配置重启）我评估过——接入频率几个月一次，远低于通知量。

### 7. 明确承诺 at-least-once，并主动为下游去重创造条件

**决策**：不追求 exactly-once；注入稳定的 `X-Notification-Id` + `X-Notification-Attempt`；
在 mockvendor 里把重复投递打印成明确的日志。

**原因**：来自否决 AI 的 exactly-once 建议。我认为**边界清楚的弱承诺比有漏洞的强承诺更可靠**。
更进一步，我刻意让重复这件事**可观测**——如果只在文档里写一句"可能重复"，
下游很可能不会真的去做幂等。让他们在演示里亲眼看到"同一个 ID 被收了两次"，
这个承诺才算真的交付了。

### 8. 崩溃也消耗重试预算

**决策**：lease 过期回收时 `attempt` +1，预算耗尽直接进死信。

**原因**：来自否决 AI 的"不增加 attempt"建议。
不这样做的话，一条能打挂进程的毒任务会被无限回收重放，把单条脏数据放大成全系统不可用。
**把故障限制在一条记录内，比对单条记录"公平"更重要。**

### 9. 不做通用 payload 转换引擎，只留一个逃生舱

**决策**：只提供 `text/template`，并在配置注释和文档里写明"这不是通用转换引擎"。

**原因**：来自否决 AI 的 JSONata/Jolt 建议。
这是我认为最容易被"做得更通用"诱惑而走错的一条边界。
payload 映射是业务语义，主战场在业务系统。放进通知服务意味着
每次供应商改字段都要改通知服务并发布，风险覆盖所有业务方。

### 10. 依赖数量压到 2 个，并为每个自研实现写明失效条件

**决策**：只用 `yaml.v3` + SQLite 驱动。路由用标准库 `ServeMux`，日志用 `slog`，
ID 和 Prometheus 指标自己实现。

**原因**：依赖不是免费的。同时我坚持给自研实现写**失效条件**
（如"需要 exemplar 时就换 client_golang"），
因为一个没有失效条件的技术决策，在半年后会变成没人敢动的历史包袱。

### 11. 死信不自动重投

**决策**：死信只能人工重投，没有定时自动扫描重试。

**原因**：来自否决 AI 建议。进死信意味着自动重试已经失败过了，
再加一层自动重试只是推迟同样的失败，同时掩盖"有东西坏了需要人看"这个信号。
**死信应该是一个刺眼的告警，不是一个会自己消失的状态。**

### 12. 启动期强校验，让配置错误立刻失败

**决策**：`timeout >= lease_duration` 拒绝启动；引用不存在的环境变量拒绝启动；
配置字段拼错拒绝启动；模板语法错误拒绝启动；启动时扫描指向已删除 endpoint 的孤儿任务并告警。

**原因**：这些都是我从"线上症状很迷惑、排查很久"的经验倒推出来的。
比如 `timeout > lease` 会造成"请求还在飞、任务已被回收重投"的重复投递风暴，
现场表现是下游疯狂收到重复通知，但日志里每条看起来都正常——
这种问题必须在启动时就拦住，不能等到线上。

同理，带着空 token 启动成功、然后对每条通知返回 401，
比直接启动失败糟糕得多。

### 13. 测试按"验证承诺"而不是"覆盖函数"来组织

**决策**：把最多的精力投在 16 个端到端用例上，每个用例对应 README 里的一条承诺。

**原因**：这个系统的价值全部体现在跨层行为上（故障恢复、隔离、不丢消息），
单测每个函数都绿但系统整体不可靠是完全可能的。
我让测试名本身就是规格说明（`TestCircuitBreakerStopsHammeringAndPreservesRetryBudget`），
这样任何人否决我的设计之前，会先看到一个失败的测试和它的名字。

## 四、对"AI 辅助工程"的一点总结

做完这个作业，我对 AI 在工程中的定位有三个具体感受：

**1. AI 扩大方案空间，但会系统性地偏向"更完整"而非"更合适"。**
它给出的第一版架构包含 Kafka、Redis、CDC、OTel——每一个单独看都有道理，
合起来是一个第一版根本不该有的系统。
**AI 没有"成本感"**，因为运维负担、发布风险、排查难度都不在它的损失函数里。
判断"什么不要"始终是人的工作。

**2. AI 对细节的建议需要按"代价是否对称"来审查，而不是按"听起来是否合理"。**
本文 2.2 节里的每一条（401 重试、4xx 熔断、崩溃不计预算）听起来都合理，
但它们共同的问题是只考虑了乐观路径，没有考虑失败时的放大效应。
我发现最有效的审查方式是问一句：**"这个建议在最坏情况下会怎样？"**
这一问就能筛掉大部分看起来对的建议。

**3. AI 会在它不了解前提的地方自信地给错答案。**
Outbox + CDC 那条是最典型的——它是一个正确的模式，但用在了错误的位置。
如果我不知道 Outbox 解决的究竟是什么问题，很可能就把它加进去了，
然后为一个不存在的问题维护一套 CDC 链路。
**这说明 AI 提升的是实现速度，而不是判断力的下限；判断力仍然只能来自自己对问题的理解。**

